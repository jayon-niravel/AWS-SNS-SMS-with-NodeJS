Example of sending an SMS with AWS SNS and NodeJS

$ npm install`

`$ npm start`

Open browser and visit something like,

`http://localhost:3000/?message=[The Message]&number=[The Number]&subject=[The Subject]`
